generator client {
  provider     = "prisma-client"
  output       = "./generated"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model Cat {
  id Int @id @default(autoincrement())

  name  String
  age   Int
  breed String

  @@map("cat")
}
